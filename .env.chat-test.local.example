# TEST ONLY. Copy to .env.chat-test.local if you want a local convenience file.
# Never copy the production .env values into this file.
GYMVYN_CHAT_TEST_LOCAL_ONLY=true
CHAT_TEST_PROJECT_REF=local
CHAT_TEST_SUPABASE_URL=http://127.0.0.1:54321
CHAT_TEST_DATABASE=gymvyn_chat_security_test

# Obtain these for the running local stack only with: supabase status -o env
# Keep the actual key out of Git and never expose it to frontend code.
